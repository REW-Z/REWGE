# 重构计划文档

## 目标

本计划用于指导当前游戏引擎从“可运行原型”逐步演进为“可持续迭代的稳定内核”。

重构原则：

1. 先止血，再重构，再扩展。
2. 优先处理会持续制造 bug 的基础设施问题。
3. 不建议直接继续堆功能。
4. 不建议完全推倒重来，建议保留已有概念与骨架，按模块做第二版重构。

---

## 一、总体优先级

按优先级分为五层：

- P0：立刻修复，属于持续制造 bug 的问题
- P1：核心架构重构
- P2：渲染架构整理
- P3：资源系统重构
- P4：工程化与可维护性整理
- P5：稳定后再做的增强功能

---

## 二、P0：立刻修复的问题

### 1. 修复 `RenderScene::RemoveFromScene()` 的索引错误

**位置：** `Src\SubSystems\RenderSystem.cpp`

**问题：**

- 目前使用了 swap-remove 删除 renderer
- 删除后没有更新被交换过来的 renderer 的 scene index

**后果：**

- `scenehandle` 会失真
- 后续更新和移除可能操作错误对象
- 场景状态会逐步损坏

**处理动作：**

- 在执行 swap-remove 后，立刻更新被搬运元素的索引

---

### 2. 修复 `UpdateAllRenderers()` 的非法移除

**问题：**

- 某些 renderer 可能尚未进入 scene
- 当前逻辑可能直接调用 `RemoveFromScene(renderer->GetNodeIdx())`

**后果：**

- 越界
- assert
- 错删对象

**处理动作：**

- 只有 `renderer->IsInScene()` 为真时才允许移除

---

### 3. 修复 `Transform` dirty 缓存失效

**位置：** `Src\Objects\BaseComponents.cpp`

**问题：**

- `GetLocalToWorldMat()` 计算完成后没有正确复位 dirty 标记

**后果：**

- 每次查询都重复计算矩阵
- 缓存机制无法发挥作用
- 依赖变换缓存的逻辑不稳定

**处理动作：**

- 计算完成后设置 `isTransformDirty = false`

---

### 4. 修复暂停逻辑导致窗口事件不更新的问题

**位置：** `Src\main.cpp`

**问题：**

- `isPaused` 后直接 `continue`
- 导致平台更新和事件轮询被跳过

**后果：**

- 窗口表现像卡死
- 用户无法正常恢复

**处理动作：**

- 暂停时仍执行平台事件更新
- 只跳过游戏逻辑更新，不跳过窗口与输入处理

---

### 5. 修复对象删除时未清理全局对象映射

**位置：** `Src\Objects\Object.cpp`

**问题：**

- `CreateInstance()` 会把对象插入全局映射
- `DeleteInstance()` 没有从映射中移除

**后果：**

- 悬空指针
- `FindObject()` 可能返回已释放对象

**处理动作：**

- 删除对象前先从全局映射注销

---

### 6. 修正世界 AABB 计算

**位置：** `Src\Objects\Renderer.cpp`

**问题：**

- `MeshRenderer::UpdateAABB()` 里的 AABB 变换算法不正确

**后果：**

- Frustum Culling 错误
- Shadow Culling 错误
- 物体可能被错误剔除或错误保留

**处理动作：**

- 改为标准 AABB 变换方式：
  - `newCenter = M * center`
  - `newExtents = abs(mat3(M)) * extents`

---

## 三、P1：核心架构重构

### 7. 建立统一的生命周期与所有权模型

**现状问题：**

- `World`、`GameObject`、`Component`、资源对象均大量使用裸指针
- 谁创建、谁持有、谁销毁不够清晰

**目标：**

- `World` 拥有 `GameObject`
- `GameObject` 拥有 `Component`
- `ResourceManager` 拥有资源对象
- `RenderScene` 只持有非拥有引用或句柄

**建议：**

- 明确每类对象的创建入口与销毁入口
- 明确注销顺序与事件解绑顺序
- 后续可逐步迁移到 `unique_ptr` 或句柄系统

---

### 8. 重做 `GameObject` / `Component` 销毁流程

**现状问题：**

- `GameObject::~GameObject()` 几乎为空
- `Renderer` 注册到全局列表后没有稳定移除路径
- 事件监听可能保留悬空调用

**目标：**

- `GameObject` 析构时释放全部组件
- 组件析构时自动从事件与渲染系统解绑
- 层级关系能安全断开

---

### 9. 重构 `Transform` 层级系统

**现状问题：**

- `SetParent()` 没有处理旧父节点解绑
- 世界矩阵缓存逻辑不完整
- 方向向量与层级传播依赖不够稳

**目标：**

- 建立规范的父子变换关系
- 拆分局部矩阵和世界矩阵缓存
- 建立稳定的 dirty 传播机制

**建议动作：**

- `SetParent()` 中补旧父节点 child 列表移除
- 明确 local/world 变换更新链条
- 所有方向函数建立在统一世界旋转或世界矩阵上

---

### 10. 让事件系统具备生命周期安全性

**现状问题：**

- `Event` 当前只保存监听者与回调
- 没有稳定的断开连接机制

**后果：**

- 很容易调用到已销毁对象

**建议动作：**

- 引入 listener token / connection 模型
- 或在对象析构时自动解绑所有监听

---

## 四、P2：渲染架构整理

### 11. 拆分 `RenderSystem.cpp`

**现状问题：**

- 单文件、单流程承担了过多职责
- 包括相机、裁剪、队列、阴影、天空盒、主渲染、OpenGL 状态切换

**目标：**

拆为更清晰的阶段模块，例如：

- SceneSync
- Cull
- QueueBuilder
- SkyboxPass
- ShadowPass
- MainForwardPass

---

### 12. 移除渲染全局变量，建立 `RenderContext`

**现状问题：**

- `mMat`、`vMat`、`pMat`、`lightVmatrix` 等是全局变量
- uniform location 也通过全局变量共享

**后果：**

- 强耦合
- 不利于多相机
- 不利于多线程或多 pass
- 调试困难

**目标：**

- 使用显式上下文对象在渲染链中传递数据

---

### 13. 做 uniform location 缓存

**现状问题：**

- 每帧 draw 时大量调用 `glGetUniformLocation`

**后果：**

- 冗余开销
- shader 调用逻辑凌乱

**目标：**

- 在 shader compile/link 后缓存 uniform 位置
- 绘制阶段只做 uniform 赋值

---

### 14. 标准化 OpenGL 状态管理

**现状问题：**

- `glEnable`、`glDisable`、`glFrontFace`、`glDepthFunc`、`glDrawBuffer` 分散在多处

**后果：**

- 状态不透明
- 易遗漏恢复
- 调试困难

**目标：**

- 最少建立轻量状态封装
- 明确每个 pass 使用的渲染状态

---

### 15. 补齐 FBO 完整性检查

**现状问题：**

- 阴影贴图和 RenderTexture 创建后没有形成稳定的检查流程

**目标：**

- 所有 FBO 创建后立即检查 completeness
- 检查失败时记录日志并终止使用该资源

---

## 五、P3：资源系统重构

### 16. 重构 `ResourceManager` 的 key 设计

**现状问题：**

- 当前资源缓存主要按 path 作为 key
- 类型约束较弱

**风险：**

- 同路径不同类型行为不明确
- 路径格式差异容易造成重复加载

**目标：**

- 资源 key 改为 `type + normalized_path`

---

### 17. 建立明确的资源释放链

**现状问题：**

- `Unload()` 逻辑过薄
- GPU 资源释放不统一

**目标：**

- `Texture`、`Shader`、`Mesh`、`FBO`、`RenderTexture` 建立清晰析构策略
- 资源卸载时确保 CPU 与 GPU 资源都能正确释放

---

### 18. 整理 `AssetPipeline` 与 `ResourceManager` 边界

**现状问题：**

- 资源导入与资源缓存边界不够清晰

**目标：**

- `AssetPipeline` 只负责导入
- `ResourceManager` 只负责缓存、查找、卸载、生命周期管理

---

## 六、P4：工程化与可维护性整理

### 19. 清理 include 依赖

**现状问题：**

- 很多 `.cpp` 包含大量无关头文件
- OpenGL/GLFW/SOIL2 头文件传染严重

**目标：**

- 能前置声明的前置声明
- `.cpp` 只 include 当前实现真正依赖的头文件

---

### 20. 移除 `.cpp` 文件中的 `#pragma once`

**问题：**

- 当前有多个 `.cpp` 文件出现 `#pragma once`
- 这是不规范写法

**目标：**

- 仅在头文件使用 `#pragma once`

---

### 21. 统一命名风格与模块边界

**现状问题：**

- 当前同时混用 Unity 风格、宏反射风格、OpenGL 过程式风格

**目标：**

- 统一模块职责
- 统一命名与代码组织方式

---

### 22. 为关键基础模块建立最小回归验证

**优先验证内容：**

- Transform 父子层级
- RenderScene 增删
- AABB 更新
- 资源重复加载与卸载
- 暂停与恢复主循环

---

## 七、P5：稳定后再推进的增强项

以下功能不建议在当前状态下优先实现：

- 多光源
- 透明物体排序
- 后处理
- 动画系统
- 粒子系统
- 序列化
- 编辑器
- 脚本热重载
- 多线程资源加载

这些功能应在基础设施稳定后再推进。

---

## 八、推荐执行顺序

### 第一阶段：先止血

1. 修复 `RenderScene::RemoveFromScene()`
2. 修复 `UpdateAllRenderers()` 的非法移除
3. 修复 `Transform` dirty 缓存
4. 修复 pause 导致事件不更新的问题
5. 修复对象删除时未清理全局映射
6. 修复世界 AABB 计算

---

### 第二阶段：稳定核心架构

1. 建立生命周期与所有权模型
2. 重做 `GameObject` / `Component` 销毁链
3. 重构 `Transform` 层级系统
4. 为事件系统加入安全解绑机制

---

### 第三阶段：整理渲染系统

1. 拆分 `RenderSystem.cpp`
2. 建立 `RenderContext`
3. 做 uniform location 缓存
4. 标准化 GL 状态管理
5. 加入 FBO 完整性检查

---

### 第四阶段：整理资源系统与工程结构

1. 重构 `ResourceManager` key 设计
2. 建立资源释放链
3. 整理 `AssetPipeline` 边界
4. 清理 include
5. 移除 `.cpp` 中的 `#pragma once`
6. 统一命名风格与模块边界

---

## 九、结论

当前引擎不适合继续无序叠加功能。

最优策略不是完全推倒重来，而是：

**保留已有概念骨架，优先修复 P0 问题，再按 P1 ~ P4 逐步推进核心重构。**

真正最先要动的，不是新功能，而是以下四项基础设施：

1. 生命周期
2. Transform
3. Scene 索引管理
4. 渲染上下文

这四部分稳定后，后续功能开发成本才会显著下降。
